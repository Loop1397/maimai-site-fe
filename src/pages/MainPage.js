const MainPage = () => {
    return (
        <div>
            Hi React!!
            <button onClick={() => {console.log("button Clicked!!")}}>
                버튼
            </button>
        </div>
    );
};

export default MainPage;